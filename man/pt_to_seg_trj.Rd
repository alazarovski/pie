% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pt_to_seg_trj.R
\name{pt_to_seg_trj}
\alias{pt_to_seg_trj}
\title{Build segment trajectories from point trajectories}
\usage{
pt_to_seg_trj(
  trj_data,
  cols = list(fl_id = "fl_id", seq_id = NULL, lon = "lon", lat = "lat", altitude =
    "altitude", time_over = "time_over", adep = "adep", ades = "ades"),
  order_by = c("seq_id", "time_over", "none"),
  keep_cols = NULL,
  drop_incomplete = TRUE,
  compute_distance = FALSE,
  compute_duration = FALSE,
  make_segment_id = TRUE
)
}
\arguments{
\item{trj_data}{A \code{data.frame} or \code{data.table} with point trajectories.}

\item{cols}{Named list mapping \strong{canonical} names used internally to the
column names present in \code{trj_data}. Minimum required keys:
\code{fl_id}, \code{seq_id}, \code{lon}, \code{lat}. Optional: \code{altitude}, \code{time_over}, \code{adep}, \code{ades}.
Example:
\code{list(fl_id="flight_id", seq_id="seq", lon="longitude", lat="latitude", altitude="fl", time_over="ts", adep="dep", ades="arr")}.}

\item{order_by}{One of \code{c("seq_id","time_over","none")}. How to order points within each flight
before segmenting. Default \code{"seq_id"}.}

\item{keep_cols}{Character vector of \strong{additional} columns in \code{trj_data} to carry
through (from the entry point row). Use when you want to preserve extra attributes.}

\item{drop_incomplete}{Logical, default \code{TRUE}. If \code{TRUE}, drops the last point
of each flight that lacks an exit (lead) point, i.e., segments with \code{NA} exit.}

\item{compute_distance}{Logical, default \code{FALSE}. If \code{TRUE}, computes 2D great-circle
distance per segment (meters and nautical miles) using \code{geosphere::distHaversine()}.}

\item{compute_duration}{Logical, default \code{FALSE}. If \code{TRUE} and \code{time_over} is present,
computes segment duration in seconds.}

\item{make_segment_id}{Logical, default \code{TRUE}. If \code{TRUE}, creates a \code{seg_id}
of the form \code{"<fl_id>:<e_seq_id>><x_seq_id>"}.}
}
\value{
A \code{data.table} with one row per segment containing:
\itemize{
\item \code{fl_id}, \code{e_seq_id}, \code{x_seq_id}
\item entry/exit coordinates: \code{e_lon}, \code{e_lat}, \code{x_lon}, \code{x_lat}
\item optionally \code{e_alt}, \code{x_alt}, \code{e_time}, \code{x_time}, \code{adep}, \code{ades}
\item optional computed metrics (\code{dist_m}, \code{dist_nm}, \code{dur_s})
\item optional \code{seg_id}
\item any \code{keep_cols} copied from the entry row
}
}
\description{
Converts a point-wise trajectory table (one row per point) into a segment-wise
table (one row per adjacent point pair) per flight. Columns may have arbitrary
names in the input; map them via \code{cols}.
}
\section{Column mapping}{

Canonical names used internally: \code{fl_id}, \code{seq_id}, \code{lon}, \code{lat},
optional \code{altitude}, \code{time_over}, \code{adep}, \code{ades}. Map them from your input
once via \code{cols}; the function renames on a \strong{copy} and never mutates \code{trj_data}.
}

\examples{
\dontrun{
library(data.table)
pts <- data.table(
  flight = c(1,1,1, 2,2),
  seq    = c(1,2,3, 1,2),
  lon    = c(0, 0.5, 1.0, 0,  1),
  lat    = c(0, 0.0, 0.0,  1,  1),
  ts     = as.POSIXct("2024-01-01 00:00:00", tz="UTC") + c(0,60,120, 0, 90)
)

seg <- pt_to_seg_trj(
  pts,
  cols = list(fl_id="flight", seq_id="seq", lon="lon", lat="lat", time_over="ts"),
  order_by = "seq_id",
  compute_distance = TRUE,
  compute_duration = TRUE
)
seg[]
}

}
